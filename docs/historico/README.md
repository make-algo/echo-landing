# Histórico de diseño (v2, pre-corrección)

Migrado desde `make-algo/echo` (repo privado), donde vivía duplicado y sin sincronizar desde el
13-09-2026 — MAK-160, petición de Álvaro de no mantener el código de la landing en dos
repositorios.

Estos tres ficheros son el mockup estático y los tokens de la **v2** del diseño de la landing
(acento verde-petróleo, MAK-74/MAK-77), la dirección que perdió el bucle de crítica externa frente
a la "corrección" (roja, tachados a mano) en MAK-71/MAK-85. **No describen el sitio que está en
producción.** Se conservan solo como referencia histórica de por qué se tomaron ciertas decisiones
de contraste y escala — el razonamiento en `landing.md` del repo `make-algo/echo` sigue siendo
válido como documento de decisión, aunque el CSS y el HTML aquí ya no correspondan al sitio real.

La fuente de verdad viva de tokens y composición es `../../src/styles/` y `../../src/pages/` en
este mismo repo.
