---
import WaitlistForm from '../WaitlistForm.astro'
import { beta } from '../../content/copy'

/** 8 · Lista de espera. La única banda de la página. */
---

<section id="beta" class="section band">
  <div class="wrap beta">
    <h2>{beta.titulo}</h2>
    <p class="lead">{beta.entradilla}</p>
    <WaitlistForm />
  </div>
</section>
